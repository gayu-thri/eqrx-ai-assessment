[project]
name = "factory-intelligence"
version = "0.1.0"
description = ""
authors = [
    {name = "Gayathri Ethiraj",email = "egayu3@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli] (>=1.0.0)",
    "psycopg[binary,pool] (>=3.1.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "black (>=26.3.1,<27.0.0)",
    "isort (>=8.0.1,<9.0.0)"
]

[tool.poetry]
packages = [{include = "factory_intelligence", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
